{
  "name": "paddle-rotation",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@netlify/blobs": "latest"
  },
  "scripts": {
    "test": "node --test tests/*.test.cjs"
  }
}
